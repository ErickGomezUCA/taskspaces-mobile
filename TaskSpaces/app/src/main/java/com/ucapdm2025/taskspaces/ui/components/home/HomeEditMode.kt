package com.ucapdm2025.taskspaces.ui.components.home

/**
 * Enum class representing the different modes for editing a workspace.
 *
 * These modes are used to determine the action to be performed on a workspace,
 * such as updating or deleting it.
 */
enum class HomeEditMode {
     NONE,
    UPDATE,
    DELETE,
}